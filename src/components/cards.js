import { el } from '../utils/dom.js';
import { routes } from '../config/routes.js';
import { getProjectUrl } from '../services/external-channel.service.js';

export function projectCard(project) { const href = project.linkKey === 'tlatolli' ? routes.tlatolli : getProjectUrl(project.linkKey); const action = href ? el('a', { className: 'button ghost', href, target: href.startsWith('http') ? '_blank' : undefined, rel: href.startsWith('http') ? 'noopener noreferrer' : undefined, text: project.linkKey === 'tlatolli' ? 'Ir a Tlatolli →' : `Conocer ${project.name} →` }) : el('button', { className: 'button ghost', type: 'button', disabled: true, text: 'Próximamente' }); return el('article', { className: `project-card${project.id === 'cuicoyan' ? ' featured' : ''}` }, [el('div', { className: 'project-mark', text: project.name.slice(0, 2).toUpperCase(), attrs: { 'aria-hidden': 'true' } }), el('span', { className: 'demo', text: project.status }), el('h3', { text: project.name }), el('p', { className: 'muted', text: project.tagline }), el('p', { text: project.description }), el('div', { className: 'card-footer' }, [el('small', { className: 'muted', text: 'Estado público por verificar' }), action])]); }
export function principleCard(item, index) { return el('article', { className: 'principle-card' }, [el('div', { className: 'principle-icon', text: item.icon, attrs: { 'aria-hidden': 'true' } }), el('span', { className: 'muted', text: `0${index + 1}` }), el('h3', { text: item.title }), el('p', { className: 'muted', text: item.description })]); }
export function impactCard(item) { return el('article', { className: 'impact-card' }, [el('span', { className: 'impact-number', text: item.icon }), el('h3', { text: item.title }), el('p', { className: 'muted', text: item.description })]); }
export function articleCard(item) { return el('article', { className: 'article-card' }, [el('span', { className: 'badge', text: item.category }), el('h3', { text: item.title }), el('span', { className: 'status', text: item.status })]); }
